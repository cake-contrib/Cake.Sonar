# Initalize
init:
  - git config --global core.autocrlf true

image: Ubuntu

build_script:
  - sh: ./build.sh --target="appveyor"

test: on

branches:
  only:
    - master
    - develop

skip_tags: true
